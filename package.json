{
  "name": "jwt-api-lambda",
  "version": "1.0.0",
  "main": "handler.js",
  "scripts": {
    "start": "node app.js",
    "test": "jest --runInBand",
    "test:watch": "jest --watch",
    "build": "echo 'No build step required for this Lambda function'",
    "deploy": "serverless deploy",
    "deploy:dev": "serverless deploy --stage dev",
    "deploy:prod": "serverless deploy --stage prod"
  },
  "dependencies": {
    "dotenv": "^16.5.0",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.0",
    "serverless-http": "^3.2.0"
  },
  "devDependencies": {
    "jest": "^30.0.0",
    "supertest": "^7.1.1"
  }
}
